<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Profilo Operatore</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-5">
    <h2>Profilo</h2>

    <div class="card p-3">
        <p><strong>Nome:</strong> <span th:text="${operatore.name}"></span></p>
        <p><strong>Email:</strong> <span th:text="${operatore.email}"></span></p>
        <p><strong>Stato attuale:</strong> <span th:text="${operatore.statoOperatore}"></span></p>

        <form th:action="@{/profilo/update-stato}" method="post">
            <div class="mb-3">
                <label for="nuovoStato">Cambia stato:</label>
                <select name="nuovoStato" class="form-select">
                    <option value="ACTIVE">Attivo</option>
                    <option value="NO_ACTIVE">Non attivo</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Aggiorna</button>
        </form>

        <div class="text-danger mt-2" th:if="${errore}" th:text="${errore}"></div>
        <div class="text-success mt-2" th:if="${successo}" th:text="${successo}"></div>
    </div>
</div>
</body>
</html>
